# Build output
/dansal
/dansal_web
/web
/dansal_webmin
/dansal_admin
/dansal_doc
cmd/dansal/dansal
cmd/dansal_web/dansal_web
*.deb

# Admin socket
*.sock

# Database
*.db

# Images directory (uploaded event images — not wiki screenshots)
images/
!wiki/images/

# Environment / config overrides
*.env
.env

# Instance config files — contain secrets (SMTP, tokens, session keys).
# Templates live under deploy/; real configs go in /etc/dansal/<instance>/.
config.yaml
web.yaml
webmin.yaml

# Editor
.idea/
.vscode/
*.swp
*.swo
dansal-events-embed.zip
