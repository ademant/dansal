[Unit]
Description=Dansal feed fetch (%i)
After=dansal@%i.service
Requires=dansal@%i.service

[Service]
Type=oneshot
User=dansal
Group=dansal
ExecStart=/usr/bin/dansal_admin --socket /run/dansal/%i/dansal.sock fetch-all
NoNewPrivileges=true
