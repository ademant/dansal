[dansal-web]
enabled  = true
filter   = dansal-web
backend  = systemd
journalmatch = _SYSTEMD_UNIT=dansal-web.service
# Ban after 5 matched log lines within 10 minutes; adjust to match
# login_max_failures / login_window_mins in web.yaml.
maxretry = 5
findtime = 600
bantime  = 3600
