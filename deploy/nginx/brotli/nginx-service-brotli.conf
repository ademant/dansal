# Runs (as root, "+") before every nginx start/restart: if the brotli module
# no longer matches the installed nginx, disable it so nginx starts with gzip
# only instead of failing on load_module. Never builds here (start timeout).
[Service]
ExecStartPre=+/usr/local/sbin/nginx-brotli-ensure check
